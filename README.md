# DeepLearning-CS260C-HW1
Building a multi-class image classification pipeline to classify images (from MNIST and CIFAR-10 datasets) using fully-connected neural networks (MLP). Practice implementing convolutional layers, and train convolutional neural networks on MNIST and CIFAR-10 for image classification.
